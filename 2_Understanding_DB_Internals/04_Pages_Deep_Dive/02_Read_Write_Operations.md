# 🔄 Read and Write Operations

> Database lo read/write ela jaragutayi? Buffer Pool tho!

---

## 📖 READ Operation Flow

```sql
SELECT * FROM employees WHERE id = 100
```

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   Step 1: Query comes                                  │
│   "Give me employee with id = 100"                     │
│                                                         │
│   Step 2: Check Buffer Pool                            │
│   ┌──────────────────────────────────────────────┐     │
│   │  Buffer Pool: Page 5 undi? 🤔                │     │
│   │                                               │     │
│   │  ┌───────┐ ┌───────┐ ┌───────┐              │     │
│   │  │Page 1 │ │Page 3 │ │Page 7 │  ← Page 5 ❌  │     │
│   │  └───────┘ └───────┘ └───────┘              │     │
│   └──────────────────────────────────────────────┘     │
│                                                         │
│   Step 3: Page 5 ledu! Disk nundi techukovaali        │
│   ┌──────────────────────────────────────────────┐     │
│   │  DISK: [Page 1][Page 2][Page 3][Page 4][Page 5]│    │
│   └──────────────────────────────────────────────┘     │
│                                               ↑         │
│                      Read Page 5 ─────────────┘         │
│                                                         │
│   Step 4: Page 5 Buffer Pool lo pettu                  │
│   ┌──────────────────────────────────────────────┐     │
│   │  ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐    │     │
│   │  │Page 1 │ │Page 3 │ │Page 7 │ │Page 5 │ ✅ │     │
│   │  └───────┘ └───────┘ └───────┘ └───────┘    │     │
│   └──────────────────────────────────────────────┘     │
│                                                         │
│   Step 5: Return row from Page 5                       │
│   Employee 100 found! ✅                                │
│                                                         │
│   BONUS: Next time Page 5 kavali ante, already         │
│          memory lo undi! No disk IO! ⚡                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## ✏️ WRITE Operation Flow

```sql
UPDATE employees SET salary = 60000 WHERE id = 100
```

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   Step 1: Find Page 5 (same as read)                   │
│   (Page 5 has employee 100)                            │
│                                                         │
│   Step 2: Modify page IN MEMORY only!                  │
│   ┌──────────────────────────────────────────────┐     │
│   │  Buffer Pool:                                 │     │
│   │  ┌───────────────────────────────────┐       │     │
│   │  │ Page 5 (DIRTY! 🟡)                 │       │     │
│   │  │                                    │       │     │
│   │  │ Employee 100:                      │       │     │
│   │  │   salary: 50000 → 60000 ✏️         │       │     │
│   │  │                                    │       │     │
│   │  │ Modified in memory, not disk yet! │       │     │
│   │  └───────────────────────────────────┘       │     │
│   └──────────────────────────────────────────────┘     │
│                                                         │
│   Step 3: Write to WAL (Write-Ahead Log)               │
│   ┌──────────────────────────────────────────────┐     │
│   │  WAL (on disk):                               │     │
│   │  "Page 5, Employee 100, salary = 60000"      │     │
│   │                                               │     │
│   │  ✅ This IS written to disk!                  │     │
│   │  Purpose: Crash recovery                      │     │
│   └──────────────────────────────────────────────┘     │
│                                                         │
│   Step 4: Return SUCCESS to user                       │
│   "Update complete!" (even though disk not updated!)  │
│                                                         │
│   Step 5: Eventually - Checkpoint (Background)         │
│   Dirty pages disk ki flush chestundi                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🟡 Dirty Pages

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   DIRTY PAGE = Modified in memory, not saved to disk   │
│                                                         │
│   ┌───────┐ ┌───────┐ ┌───────┐                        │
│   │Page 1 │ │Page 5 │ │Page 3 │                        │
│   │CLEAN ✅│ │DIRTY🟡│ │CLEAN ✅│                        │
│   └───────┘ └───────┘ └───────┘                        │
│       ↓         ↓         ↓                            │
│   Same as   Different  Same as                         │
│    disk      from disk   disk                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📝 WAL (Write-Ahead Log)

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   WAL = Safety net!                                    │
│                                                         │
│   Why WAL?                                             │
│   ─────────                                             │
│   If server crashes with dirty pages in memory,        │
│   data is LOST! 😱                                      │
│                                                         │
│   Solution:                                            │
│   ─────────                                             │
│   1. Write change to WAL FIRST (quick, sequential)     │
│   2. Then modify memory                                │
│   3. WAL is on disk = survives crash                   │
│   4. On restart, replay WAL to recover                 │
│                                                         │
│   WAL = Insurance for your data! 🛡️                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Checkpoint Process

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   Checkpoint = Flush dirty pages to disk               │
│                                                         │
│   Before Checkpoint:                                   │
│   - Memory: Page 5 (salary=60000) 🟡                   │
│   - Disk: Page 5 (salary=50000) ❌                     │
│                                                         │
│   After Checkpoint:                                    │
│   - Memory: Page 5 (salary=60000) ✅                   │
│   - Disk: Page 5 (salary=60000) ✅                     │
│                                                         │
│   Now page is CLEAN again!                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Why This Design?

| If we wrote to disk immediately | With Buffer Pool + WAL |
|--------------------------------|------------------------|
| Every write = Disk IO (slow) | Writes go to memory (fast) |
| Random disk writes | Sequential WAL writes (fast) |
| Many small disk operations | Batched disk operations |
| 🐢 SLOW | ⚡ FAST |

---

## 🌟 Key Points (Remember!)

```
✅ READ: Check buffer pool first, then disk
✅ WRITE: Modify memory, write WAL, flush later
✅ Dirty Page = Modified but not saved
✅ WAL = Crash recovery insurance
✅ Checkpoint = Save dirty pages to disk
```

---

## ➡️ Next: [Page Content Types](./03_Page_Content_Types.md)
